#include "vex.h"

void testvision (color col, signature sig) {
   findGoal(blue, BLUEGOAL, false);
  //setDrivetrainSpeed(-3.5, 3.5);
}

void testflywheel (color col, signature sig) {
  int area = findGoal(blue, BLUEGOAL, false);
  printf("area %i \n", area);
  
}